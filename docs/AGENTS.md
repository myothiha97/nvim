# Repository guidelines

**See [`CLAUDE.md`](CLAUDE.md) in this folder. It is the canonical reference and
applies to every AI agent, whatever tool you are.**

This file is a pointer, not a copy. It used to hold its own half of the
guidelines — build commands, Lua style, the Ghostty/tmux context — while
`CLAUDE.md` held the other half, and the two drifted three months apart. All of
that content now lives in `CLAUDE.md`; nothing was dropped.

Before any change, the repo root comes first:

1. [`rules.md`](../rules.md) — the discipline rules and the active config-freeze window
2. [`neovim-config-change-gate.md`](../neovim-config-change-gate.md) — the necessity
   check to apply to every change request
3. [`AGENTS.md`](../AGENTS.md) / [`CLAUDE.md`](../CLAUDE.md) — the root entry points
